# Set the prefix to C-j and free the original C-b keybinding
set -g prefix C-j
unbind C-b

# Set the delay between prefix and command
set -s escape-time 1

# Set the base index for windows and panes to 1 instead of 0
set -g base-index 1
set -g pane-base-index 1

